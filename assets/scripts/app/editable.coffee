# jQuery ($) -> 
#   editables = $ "[data-edit='edit']"
#   do editables.editable
#   images    = editables.find ".image"
#   images.editable "option", "display", (value) ->
#     img = ($ this).first "image"
