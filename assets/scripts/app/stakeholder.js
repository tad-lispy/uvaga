// Generated by CoffeeScript 1.6.3
(function() {
  jQuery(function($) {
    ($("#groups")).selectize({
      delimiter: ";",
      create: function(input) {
        return {
          text: input,
          value: input
        };
      }
    });
    return ($("select#shape, select#color, select#background")).each(function(i, select) {
      var $select;
      $select = $(select).hide().change(function() {
        var src;
        src = "/avatars/" + $("#shape").val() + "/" + $("#color").val() + "/" + $("#background").val() + "/" + $("#avatar").width();
        return ($("#avatar")).attr("src", src);
      });
      $select.before($("<a class='btn next'>\n  <i class='icon-chevron-left'> </i>\n</a>").click(function() {
        var next;
        next = $select.find("option:selected").next();
        if (!next.length) {
          next = $select.find("option:eq(0)");
        }
        next.prop("selected", true);
        return $select.change();
      }));
      $select.after($("<a class='btn prev'>\n  <i class='icon-chevron-right'> </i>\n</a>").click(function() {
        var next;
        next = $select.find("option:selected").prev();
        if (!next.length) {
          next = $select.find("option:eq(-1)");
        }
        next.prop("selected", true);
        return $select.change();
      }));
      return $select.change();
    });
  });

}).call(this);

/*
//@ sourceMappingURL=stakeholder.map
*/
